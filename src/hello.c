#include <stdio.h>

/**
 * Prints a hello message, waits for the user to press Enter.
 */
int main(void)
{
    puts("Hello Bitmap Brothers!");
    puts("Press Enter...");
    getchar();
    return 0;
}
